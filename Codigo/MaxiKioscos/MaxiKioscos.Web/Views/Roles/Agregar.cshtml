@using MaxiKioscos.Entidades
@using MaxiKioscos.Web.Comun.Extensiones
@model int?

@Html.PopupTitulo("Agregar reorte a rol")
@using (Html.BeginForm("Agregar", "Roles"))
{
    <div class="modal-body">
        <div class="row">
            <div class="col-lg-12">
                <div class="col-lg-6">
                    <div class="form-group">
                        @Html.Label("Reporte")
                        @Html.DropDownList("ReporteId", new SelectList(ViewBag.ListadoSobrantes as List<Reporte>, "ReporteId", "NombreCompleto", Model), "Seleccione Reporte", new { @data_searchable = "true", @class = "form-control" })
                        @Html.ValidationMessage("ReporteId")
                        @Html.Hidden("RoleId", (int)ViewBag.RoleId)
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-floppy-disk"></span>&nbsp;&nbsp;Agregar</button>
        <button type="button" class="btn btn-default" data-dismiss="modal"><span class="glyphicon glyphicon-remove"></span>Cancelar</button>
    </div>
}

